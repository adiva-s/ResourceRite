<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Forgot Password</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: #f7f7f7;
            text-align: center;
            padding-top: 100px;
        }
        .card {
            background: white;
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            width: 400px;
            margin: auto;
        }
        input {
            width: 90%;
            padding: 10px;
            margin-top: 15px;
            border: 1px solid #ccc;
            border-radius: 6px;
        }
        button {
            margin-top: 20px;
            background-color: #2a6fd7;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 6px;
            cursor: pointer;
        }
        button:hover {
            background-color: #1f54a3;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>Reset Your Password</h1>
        {{#if message}}
            <p style="color: #333; font-size: 14px; background-color: #e6f4ff; padding: 10px 15px; border-radius: 6px; border: 1px solid #91caff;">
            {{message}}<br><br>
            📩 If you don’t see it, please check your <strong>Spam</strong> or <strong>Promotions</strong> folder.
              </p>
        {{/if}}
        <form action="/auth/forgot" method="POST">
            <label for="email">Enter your email address:</label><br>
            <input type="email" name="email" required /><br>
            <button type="submit">Send Reset Link</button>
        </form>
    </div>
</body>
</html>
